# Global Macro Dashboard

Desktop dashboard combining U.S. rates and inflation indicators. It brings together Fed Funds history, headline and core CPI, Truflation, FedWatch probabilities, the 10Y-2Y Treasury spread and a recession-probability series.

Run:

```bash
python taux.py
```

The module keeps a local cache in `.cache/` to reduce repeated downloads. The cache is ignored by Git.
